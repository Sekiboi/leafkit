[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "leafkit"
version = "0.15.0b1"
description = "Offline PDF page toolkit with CLI — free forever, no paid tier."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "Sekiboi" }]
keywords = ["pdf", "merge", "split", "extract", "rotate", "offline", "freeware"]
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Operating System :: Microsoft :: Windows",
  "Programming Language :: Python :: 3",
  "Topic :: Office/Business",
  "Environment :: Win32 (MS Windows)",
]
dependencies = [
  "pypdf>=5.0.0",
  "customtkinter>=5.2.0",
  "Pillow>=10.0.0",
  "tkinterdnd2>=0.3.0",
  "pymupdf>=1.24.0",
  "cryptography>=3.1",
]

[project.scripts]
leafkit = "leafkit.cli:main"
leafkit-gui = "leafkit.app:main"

[project.urls]
Homepage = "https://github.com/Sekiboi/leafkit"
Repository = "https://github.com/Sekiboi/leafkit"
Issues = "https://github.com/Sekiboi/leafkit/issues"

[tool.setuptools.packages.find]
include = ["leafkit*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
