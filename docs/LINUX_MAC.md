# Linux and macOS

These platforms are documented separately:

- **[Linux](LINUX.md)** — apt/dnf, tarball, AppImage  
- **[macOS](MAC.md)** — python.org/Homebrew, `.app`, Gatekeeper  

Both are **untested on real hardware**. Windows Setup is the supported install.
